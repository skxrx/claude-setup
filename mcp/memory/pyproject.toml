[project]
name = "harness-memory"
version = "0.1.0"
description = "Semantic + investigation memory MCP server (Postgres/pgvector)"
requires-python = ">=3.11"
dependencies = [
    "mcp>=2.0.0",
    "psycopg[binary]>=3.2",
    "httpx>=0.27",
    "fastembed>=0.8.0",
]
